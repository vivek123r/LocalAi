# Node / RN
node_modules/
npm-debug.log
yarn-error.log
# NOTE: package-lock.json is intentionally COMMITTED — `npm ci` and the
# GitHub Actions npm cache both require it. Only yarn.lock stays ignored.
yarn.lock

# Android
android/.gradle/
android/app/build/
android/build/
android/local.properties
*.keystore
!android/app/debug.keystore
*.jks

# iOS
ios/Pods/
ios/build/
*.xcworkspace
!default.xcworkspace

# Metro / Hermes
.metro-health-check*

# Env / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Model binaries (never commit 700MB gguf)
*.gguf
*.bin
models/
